# 竞速赛测试 26S

## 概述

本项目基于指导书要求，完成竞速赛赛道测试，实测单圈成绩 **26.81秒**。使用者仅需修改 `trace_model.c` 即可完成适配。

## 目录结构

```
├── trace_model.c      # 轨迹模型（唯一需修改的文件）
├── ...
```


## 使用方式

1. 将本仓库代码部署至车载主控
2. **仅需打开 `trace_model.c`**，根据实际赛道微调以下宏定义：
3. 其余文件无需改动，编译烧录后即可运行

## 测试成绩

| 项目 | 结果 |
|------|------|
| 赛道类型 | 标准竞速赛道（参照指导书附录 A） |
| 单圈用时 | **26 秒** |
| 稳定性 | 全程未脱轨 |

## 注意事项

- 修改 `trace_model.c` 前请先通读指导书
- 建议首次运行时以较低目标速度验证循迹稳定性


